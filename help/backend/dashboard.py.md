# File: `backend/dashboard.py`

Dashboard-specific APIs.

Endpoints:

- `GET /api/dashboard-expiry`
- `GET /api/dashboard-notifications`
- `GET /api/dashboard-access`

Purpose:

- Supplies role-based module list for sidebar.
- Supplies permission list for frontend action controls.
- Supplies notification feed and expiry-risk rows.
