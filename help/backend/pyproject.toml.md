# File: `backend/pyproject.toml`

Poetry config for backend dependencies.

Dependencies used:

- `fastapi`: API framework
- `uvicorn`: server to run FastAPI
- `sqlalchemy`: ORM
- `psycopg2-binary`: PostgreSQL driver

Python version:

- `^3.11`
