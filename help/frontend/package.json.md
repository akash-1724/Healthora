# File: `frontend/package.json`

Frontend dependency + script config.

Dependencies:

- `react`, `react-dom`
- `react-router-dom`

Dev dependency:

- `vite`

Scripts:

- `npm run dev` starts app on `3000`
- `npm run build` creates production build
